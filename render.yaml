services:
  - type: web
    name: hitna-gestion
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
      - key: DATABASE_URL
        value: "postgresql://postgres.pngyburrixrfaxwbqsny:hitna-gestion_db@db.pngyburrixrfaxwbqsny.supabase.co:6543/postgres?sslmode=require"
      - key: SECRET_KEY
        generateValue: true